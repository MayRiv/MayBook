@if(Request.IsAuthenticated) {
    <text>Добро пожаловать, <strong>@ViewBag.Name!</strong>
    [ @Html.ActionLink("Выход", "LogOff", "Account") ]</text>
}
else {
    @:[ @Html.ActionLink("Вход", "LogOn", "Account") ]
}
